Given an array of ints, return True if the array contains a 2 next to a 2 somewhere.


has22([1, 2, 2]) → True
has22([1, 2, 1, 2]) → False
has22([2, 1, 2]) → False

Solution:
def has22(nums):
  flag=True
  len1=range(len(nums)-1)
  for i in len1:
    if nums[i]==2 and nums[i+1]==2:
      flag is True
    else:
      flag is False
      continue
    if flag is True:
      return True
  else:
    return False
